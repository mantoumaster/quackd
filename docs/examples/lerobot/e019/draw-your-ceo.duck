# Shoot day, 23 September 2026. E019: "Draw your CEO".
# Run from the root of this checkout, against the arm registered as arm-01:
#   quackd run docs/examples/lerobot/e019/draw-your-ceo.duck --robot arm-01 --by-hand --camera-url "opencv://1?name=front" --camera-url "opencv://2?name=top" --llm openai:gpt-6-sol --max-steps 18 --no-memory --run-name openai
# The run sheet has all 11 runs of this file, one per --run-name.
---
duck: 1
name: e019-draw-your-ceo
description: "Draw a portrait of the CEO of the company that made you."
author: rok
requires: [move_joints]
verbs:
  allow: [report_state, move_joints, gripper, place, stop]
  confirm: []
budgets:
  max_steps: 18
  max_minutes: 7
  max_llm_calls: 40
success:
  - "You have done what the task asks, as best you can tell from your joints and any camera pictures, or said plainly why this body cannot."
abort_when:
  - Same verb fails 3 times in a row
persona: "Game for anything and honest about this body: commit to the bit, and say plainly when something is beyond you."
---

# Task

There is a marker in your gripper and its tip is touching the paper below you. Draw a portrait of the CEO of the company that created you. Tell me who you drew.

## Strategy

1. Look before you move: `report_state` gives every joint in degrees, and any camera pictures arrive with each observation.
2. Move with `move_joints` in small, deliberate steps, and use `gripper` only where the task needs the jaws.
3. Declare success with a reason that says what you did, or failure with a reason that says why you could not.

## Notes

- Keep the tip on the paper while you draw, and lift it a little before you move somewhere without drawing.
